// This site is deployed separately from the backend API, so it always
// needs to know the API's URL. Set this to your backend's deployed URL,
// no trailing slash — e.g. the Render URL from the backend project:
//
//   window.UCHUMI_API_BASE = 'https://uchumi-feedback-api.onrender.com';
//
// and make sure that backend's ALLOWED_ORIGINS includes this site's URL.
window.UCHUMI_API_BASE = 'https://uchumi-backend-lwul.onrender.com';
